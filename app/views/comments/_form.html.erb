<%= render 'layouts/errors', :object => @comment %>

<%= form_for [@product, @comment] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :author %>
  <%= f.text_field :author %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <p><%= f.submit %></p>
<% end %>
